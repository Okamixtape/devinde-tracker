'use client';

import React from 'react';
import Dashboard from './Dashboard';

/**
 * Page principale du tableau de bord
 *
 * Intègre le nouveau tableau de bord complet qui offre une vue unifiée
 * des données du freelance: vue d'ensemble, services, impact tarifaire,
 * simulation fiscale et dashboard financier.
 */
export default function DashboardPage() {
  return <Dashboard />;
}