/**
 * Composant de débogage de l'authentification - TEMPORAIREMENT DÉSACTIVÉ
 * 
 * Ce composant affiche l'état d'authentification actuel pour aider au débogage.
 * Il a été désactivé pour éviter de contribuer aux boucles de redirection.
 */
import React from 'react';

const AuthDebugger: React.FC = () => {
  // Retourne null pour désactiver complètement le composant
  return null;
};

export default AuthDebugger;
