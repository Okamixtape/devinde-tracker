import { ILocalStorageService } from './service-interfaces';

/**
 * Interface for secure browser localStorage operations
 * Provides secure access to browser's localStorage with encryption and additional security features
 */
export type { ILocalStorageService };