/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Activez les fonctionnalités expérimentales si nécessaire
  },
  // Vous pouvez ajouter d'autres configurations spécifiques ici
};

module.exports = nextConfig;
